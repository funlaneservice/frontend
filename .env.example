NEXT_PUBLIC_API_BASE_URL = 'https://backend-0u43.onrender.com/api'
NEXT_PUBLIC_RESTCOUNTRIES_API_KEY = ''

# Google OAuth 2.0 Web Client ID (Google Cloud Console → APIs & Services → Credentials).
# Must match the backend's GOOGLE_CLIENT_ID exactly — the backend verifies the ID token's
# audience claim against it. No client secret or redirect URI is needed for this flow.
NEXT_PUBLIC_GOOGLE_CLIENT_ID = ''
